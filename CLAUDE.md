# Jain — Claude Code Instructions

## Branching

- **Never commit or push directly to `main`.**
- All work must be done on a feature branch (e.g. `feature/<topic>` or `fix/<topic>`).
- Open a Pull Request to merge into `main` — do not merge locally.
